#!/usr/bin/env node
// optiflow CLI entry point. Requires the esbuild-bundled output
// (src/cli/index.ts -> plugin/dist/cli/index.js) so plugin installs never
// need to run a build themselves — see esbuild.config.mjs.
import "../dist/cli/index.js";
