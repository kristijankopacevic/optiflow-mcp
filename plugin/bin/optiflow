#!/usr/bin/env node
// Placeholder for the optiflow CLI entry point (src/cli/index.ts, bundled to
// plugin/dist/cli/index.js by esbuild). Phase 2+ replaces this stub with a
// require() of the built bundle.
process.exit(0);
