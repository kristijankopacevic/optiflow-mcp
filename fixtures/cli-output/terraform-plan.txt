Terraform used the selected providers to generate the following execution
plan. Resource actions are indicated with the following symbols:
  + create
  ~ update in-place
  - destroy

Terraform will perform the following actions:

  # aws_instance.api will be updated in-place
  ~ resource "aws_instance" "api" {
        id                     = "i-0abcdef1234567890"
      ~ instance_type          = "t3.medium" -> "t3.large"
        tags                   = {
            "Name" = "optiflow-api"
        }
        # (12 unchanged attributes hidden)
    }

  # aws_instance.worker will be destroyed
  - resource "aws_instance" "worker" {
      - ami                    = "ami-0123456789abcdef0" -> null
      - id                     = "i-0fedcba9876543210" -> null
      - instance_type          = "t3.small" -> null
        # (18 unchanged attributes hidden)
    }

  # aws_security_group.api_sg will be created
  + resource "aws_security_group" "api_sg" {
      + arn                    = (known after apply)
      + description            = "Managed by Terraform"
      + egress                 = (known after apply)
      + id                     = (known after apply)
      + ingress                = (known after apply)
      + name                   = "optiflow-api-sg"
      + vpc_id                 = "vpc-0a1b2c3d4e5f6a7b8"
    }

  # aws_db_instance.postgres will be updated in-place
  ~ resource "aws_db_instance" "postgres" {
        id                     = "optiflow-db"
      ~ allocated_storage      = 20 -> 50
        # (24 unchanged attributes hidden)
    }

  # module.vpc.aws_subnet.private[0] will be created
  + resource "aws_subnet" "private" {
      + arn                    = (known after apply)
      + cidr_block             = "10.0.4.0/24"
      + id                     = (known after apply)
      + vpc_id                 = "vpc-0a1b2c3d4e5f6a7b8"
    }

Plan: 2 to add, 2 to change, 1 to destroy.

Changes to Outputs:
  ~ api_instance_type = "t3.medium" -> "t3.large"
