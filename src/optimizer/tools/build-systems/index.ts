/**
 * Build Systems Tools - Cross-Platform CLI Invocation
 *
 * Only run-node-bin.ts exists here so far, pre-copied because
 * smart-package-json.ts (configuration category) depends on it. The rest of
 * this category (smart_build/smart_install/smart_lint/smart_test/
 * smart_typecheck, etc.) is not yet merged.
 */

export * from './run-node-bin.js';
